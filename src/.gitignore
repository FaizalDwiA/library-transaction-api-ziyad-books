/node_modules
/public/hot
/public/storage
/storage/*.key
/vendor
.phpunit.result.cache
docker-compose.override.yml
Homestead.json
Homestead.yaml
npm-debug.log
yarn-error.log
/.idea
/.vscode

# .env biasanya di-ignore karena mengandung secret
# Tapi untuk kemudahan testing project ini, saya izinkan commit .env
# supaya reviewer bisa langsung jalankan Docker & migrate
# Pastikan .env ini hanya untuk testing/development
# Jika production, jangan commit .env yang berisi password asli
# .env
.env.backup